---
layout: null
permalink: /feed.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/css" href="{{ '/assets/css/feed.css' | absolute_url }}"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <link>{{ '/' | absolute_url }}</link>
    <atom:link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/rss+xml"/>
    <language>en</language>
    <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
    {% for post in site.posts limit: 20 %}
    {% comment %}
      child order matters for the CSS-styled view (a browser lays them out in
      document order), so: title, date, tags, summary, url. readers don't care.
      the entity replaces undo the escaping already present in post.content so
      xml_escape doesn't double-encode it into &amp;amp;
    {% endcomment %}
    <item>
      <title>{{ post.title | xml_escape }}</title>
      <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
      {% for tag in post.tags %}<category>{{ tag | xml_escape }}</category>
      {% endfor %}
      <description>{{ post.content | strip_html | replace: '&amp;', '&' | replace: '&lt;', '<' | replace: '&gt;', '>' | replace: '&quot;', '"' | replace: '&#39;', "'" | normalize_whitespace | truncatewords: 60 | xml_escape }}</description>
      <link>{{ post.url | absolute_url }}</link>
      <guid isPermaLink="true">{{ post.url | absolute_url }}</guid>
    </item>
    {% endfor %}
  </channel>
</rss>
